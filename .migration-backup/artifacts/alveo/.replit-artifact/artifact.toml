kind = "web"
previewPath = "/"
title = "Alvéo Closet Configurator"
version = "1.0.0"
id = "artifacts/alveo"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22495

[services.development]
run = "pnpm --filter @workspace/alveo run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alveo", "run", "build" ]
publicDir = "artifacts/alveo/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22495"
BASE_PATH = "/"
